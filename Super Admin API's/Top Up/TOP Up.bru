meta {
  name: TOP Up
  type: http
  seq: 1
}

get {
  url: {{superadminapi-test}}/topup/list?search=verify
  body: none
  auth: bearer
}

params:query {
  search: verify
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
}
